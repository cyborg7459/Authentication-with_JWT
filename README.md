# Authentication-with_JWT
Very basic site with authentication using JSON web tokens, built for learning purposes.
Uses NodeJS with ExpressJS as framework and MongoDB (using Mongoose) as it's database.


